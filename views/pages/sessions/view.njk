{% extends "layouts/layout.njk" %}

{% block content %}

    <h1>{{ __('Medical report for %s', data.firstName + ' ' + data.lastName) }}</h1>
    <div>Date of birth: {{ data.birthdate }} (XX years old)</div>

    <h2>Applicant profile</h2>

    {{ data | dump }}

{% endblock %}
