{% extends "layouts/layout.njk" %}

{% block content %}
    <h1>{{ __('start.title') }}</h1>

    <p>{{ __('start.instruction_overview') }}</p>
    
    <h2 class="text-2xl mb-4">{{ __('start.how_it_works') }}</h2>

    {{ __('start.how_it_works_list') | safe }}

    <div>
        <a class="button-link w-1/3" href="{{ route('personal') }}" role="button">
            {{ __('start.submit') }}
        </a>
    </div>

    {{ __('start.lost_code_message') }}
{% endblock %}
