{%- from 'macros/validationMessage.njk' import validationMessage as validationMessage with context -%}
{%- from 'macros/input-text.njk' import textInput as textInput with context -%}
{%- from 'macros/input-textarea.njk' import textArea as textArea with context -%}
{%- from 'macros/radios.njk' import radioButtons as radioButtons with context -%}
{%- from 'macros/checkboxes.njk' import checkBoxes as checkBoxes with context -%}
{%- from 'macros/buttons.njk' import formButtons as formButtons with context -%}
{%- from 'macros/input-file.njk' import fileInput as fileInput with context -%}

<!-- custom -->
{%- from 'macros/raw-checkboxes.njk' import rawCheckBoxes as rawCheckBoxes with context -%}
{%- from 'macros/medical-consent.njk' import medicalConsent as medicalConsent with context -%}
{%- from 'macros/patientName.njk' import patientName as patientName with context -%}

<!DOCTYPE html>
<html lang="{{ getLocale() }}">
    <head>
        <!-- Required meta tags-->
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <meta name="description" content="[ALPHA] Node Starter App">
        <meta name="github-sha" content="{{ GITHUB_SHA }}">
        <meta name="csrf-token" content="{{ _csrf }}">
        <link rel="shortcut icon" href="/favicon.png" type="image/x-icon" sizes="32x32">
        <link rel="preconnect" href="https://fonts.gstatic.com/" crossorigin="">
        <link href="https://fonts.googleapis.com/css?family=Lato:700|Noto+Sans:400,700&amp;display=fallback" rel="stylesheet">
        <link rel="stylesheet" href="/build/assets/styles/app.css">
        <title>{{ __('app.title') }}</title>
    </head>
    <body>
        <div id="app">
            <a class="skip-link" href="#content">{{ __('Skip to main content') }}</a>
            <div class="outer-container">
                <header>
                    {% include "_includes/phaseBanner.njk" %}
                    {% include "_includes/fip.njk" %}
                </header>
                <main id="content">
                    {% if errors %}
                        {% include "_includes/errorList.njk" %}
                    {% endif %}

                    {% block content %}{% endblock %}
                </main>
                {% include "_includes/footer.njk" %}
            </div>
        </div>
    </body>
    {% include "_includes/script.njk" %}
    
    <script type="text/javascript" src="/build/assets/js/app.js"></script>
    
    {% block scripts %}{% endblock %}
</html>